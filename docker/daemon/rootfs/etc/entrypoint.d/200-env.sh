#!/usr/bin/env bash

env_set "DAEMON_VERSION"        "$(env_get "DAEMON_VERSION")"
env_set "DOCKER_HOST_IP"        "$(env_get "DOCKER_HOST_IP")"
env_set "PANEL_URL"             "$(env_get "PANEL_URL")"
